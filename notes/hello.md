Hello motherfucker
aÙ1
```css
export function toUrlParams(params: Record<string, unknown>): string {
	const searchParams = new URLSearchParams()

	for (const [key, value] of Object.entries(params)) {
		if (value !== undefined && value !== null) {
			searchParams.set(key, String(value))
		}
	}

	return searchParams.toString()
}

.status-bar-item.plugin-sync,
.status-bar-item.plugin-backlink,
.status-bar-item.plugin-word-count {
	display: none;
}

.status-bar-item.plugin-obsidian-git.obsidian-git-statusbar-idle.obsidian-git-statusbar-status {
	padding-top: 4px;
}

.sync-status-icon {
	display: none;
}

.app {
	background-color: transparent !important;
}

```

workspace-tab-header-container
workspace-tab-header-container,

hello
